# Local Vidbyte CLI configuration.
# Copy this file to .env for local development. Do not commit real secrets.

# Optional for non-interactive login only. The CLI exchanges this for a session
# token and never stores the raw API key.
VIDBYTE_API_KEY=vb_live_replace_with_a_local_development_key
